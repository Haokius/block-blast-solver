{
  "name": "block-blast-solver",
  "version": "1.0.0",
  "description": "A Block Blast game solver with HTML/CSS/JS frontend and Python backend",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run frontend:dev\" \"npm run backend:dev\"",
    "frontend:dev": "python3 -m http.server 3000",
    "backend:dev": "cd backend && source venv/bin/activate && python app.py",
    "install:all": "npm install && cd backend && python3 -m venv venv && source venv/bin/activate && pip install -r requirements.txt"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "keywords": ["block-blast", "solver", "puzzle", "game"],
  "author": "",
  "license": "MIT"
}
